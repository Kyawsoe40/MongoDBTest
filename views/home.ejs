<% include('./includes/header') %>
    <h1>Home Page</h1>

    <a href="/admin/create-post">Go to Admin Panel</a>
    <p>Items <%= posts.length %></p>
    <div class="post-div">
        <div class="post-box">
            <h3>Title</h3>
            <h3>Description</h3>
        </div>
        <% if(posts.length<1) { %>
            <h1>There is no post yet!</h1>
        <% }else{ 
            for(post of posts){
                %>
                <div class="post-box">
                    <span><%= post.title %></span>
                    <span><%= post.description %></span>
                </div>
        <% 
            }
        } %>
    </div>

<% include('./includes/footer') %>